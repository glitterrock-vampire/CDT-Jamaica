{
  "name": "cdt-jamaica",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend",
    "cdt-sanity"
  ],
  "scripts": {
    "start": "cd frontend && npm start",
    "build": "echo 'Build is handled by Vercel using the frontend directory directly'",
    "test": "cd frontend && npm test",
    "vercel-build": "cd frontend && npm install && npm run build"
  },
  "engines": {
    "node": ">=14.0.0"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
